language: node_js
node_js:
  - "10"
dist: trusty
sudo: required

branches:
  only:
  - dev

before_install:
  - cd ./Angular_UI && pwd

install:
  - npm install
      
script:
- npm run test -- --watch=false --no-progress --browsers=ChromeHeadlessNoSandbox